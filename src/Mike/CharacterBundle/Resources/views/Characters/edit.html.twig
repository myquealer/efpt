{% extends '::base.html.twig' %}
{% block title -%}Edit Character{% endblock %}

{% block body -%}
    <h1>Edit Character</h1>

    {{ form(edit_form) }}

    <a class="btn btn-default btn-xs" href="{{ path('characters') }}">
        Back
    </a>
    {{ form(delete_form) }}
{% endblock %}
