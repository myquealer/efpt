{% block range_widget %}
{% spaceless %}
    {{ min }}{{ form_widget(form, {'type' : 'range', 'attr' : {'min' : (min), 'max' : (max), "step" : (((max - min) / 100)|round) } }) }}{{ max }} {{ unit }}
{% endspaceless %}
{% endblock %}