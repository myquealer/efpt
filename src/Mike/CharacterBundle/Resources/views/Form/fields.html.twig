{% block range_widget %}
{% spaceless %}
    {{ min }}<input type="range" min="{{ min }}" max="{{ max }}" step="{{ ((max - min) / 100)|round }}" value="{{ value }}">{{ max }} {{ unit }}
{% endspaceless %}
{% endblock %}