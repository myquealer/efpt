{% extends '::base.html.twig' %}
{% block title -%}Character Attribute{% endblock %}

{% block body -%}
    <h1>CharacterAttributes</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ entity.value }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('characterattributes') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('characterattributes_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
